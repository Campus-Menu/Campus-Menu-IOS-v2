name: Release Build

on:
  push:
    tags:
      - 'v*.*.*'

jobs:
  build-release:
    name: Build Release
    runs-on: macos-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Xcode
      uses: maxim-lobanov/setup-xcode@v1
      with:
        xcode-version: latest-stable
        
    - name: Get version from tag
      id: version
      run: echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT
      
    - name: Update version in project
      run: |
        echo "Building version ${{ steps.version.outputs.VERSION }}"
        
    - name: Build for release
      run: |
        cd "$GITHUB_WORKSPACE"
        xcodebuild archive \
          -project "Campus Menu.xcodeproj" \
          -scheme "Campus Menu" \
          -sdk iphoneos \
          -configuration Release \
          -archivePath build/CampusMenu.xcarchive \
          CODE_SIGN_IDENTITY="" \
          CODE_SIGNING_REQUIRED=NO \
          CODE_SIGNING_ALLOWED=NO \
          DEVELOPMENT_TEAM="" \
          | xcpretty
      continue-on-error: true
      
    - name: Create Release Notes
      run: |
        cat > release_notes.md << EOF
        # Campus Menu v${{ steps.version.outputs.VERSION }}
        
        ## 🎉 Yenilikler
        
        ### Özellikler
        - ✅ Öğrenci ve Admin girişi
        - 📱 Günlük menü görüntüleme
        - ❤️ Favori yemekler
        - ⭐ Değerlendirme sistemi
        - 📅 Takvim görünümü
        - 🔔 Duyurular
        - 🌙 Dark mode desteği
        - 🌍 Türkçe/İngilizce dil desteği
        
        ### Geliştirmeler
        - Android tasarımına uygun UI
        - Performans iyileştirmeleri
        - Bug düzeltmeleri
        
        ## 📦 Kurulum
        
        1. Xcode'da projeyi açın
        2. Scheme olarak "Campus Menu" seçin
        3. Build ve Run yapın
        
        ---
        
        **Full Changelog**: https://github.com/${{ github.repository }}/compare/v${{ steps.version.outputs.VERSION }}
        EOF
        
    - name: Create GitHub Release
      uses: softprops/action-gh-release@v1
      with:
        body_path: release_notes.md
        draft: false
        prerelease: false
        files: |
          build/CampusMenu.xcarchive
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
